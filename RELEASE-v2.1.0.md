# v2.1.0：纯净药抗正式服

2026-09-13。服务端展示名称统一为 `[CN] 纯净药抗`，游戏描述为 `Zonemod药抗&单练`。取消发现标签中的 `test`，保留现有药抗规则；“正式服”是此次发布定位，并不代表更改游戏难度或关闭现有可选功能。

## 本次同步

- `jjd_browser` 修改游戏描述；`jjd_browser_maps` 将 14 个官方战役、57 个章节显示为中文，例如 `黑色狂欢节 [1/5]`。未知地图保留原代码，内部地图名和地图文件不变。
- 同步当前 `jjd_server` 1.3.0：`!vote` 进入 1V1/2V2/3V3/4V4 菜单，沿用 Match Vote。默认 4V4，12 人容量包含旁观。通过后重开当前地图。
- 保留已部署的 Confogl 2.5.0-jjd2 切换修复，防止空服切换时休眠和默认配置重入。恢复安装脚本纳入这些修复。
- 同步当前自定义源码和地图映射配置。`jjd_bot_pills.sp` 仅保留源码，本发布不新增其加载入口。
- 不安装 Anne、药役、商店或相关数据库；这些是后续讨论项。

## 部署与兼容性

先按 README 准备相同基础模组、替换地址及管理员占位符，再运行 `build-plugins.sh`、`apply-customization.py`。末尾的 `install-official.py` 安装浏览器配置并保留小人数切换修复。不要在现有服务器上运行首次安装脚本来覆盖配置。

中文地图显示使用现有 SDKTools/DHooks。当前 Linux 引擎中 `Steam3Server` 的接口指针偏移为 4，`ISteamGameServer::SetMapName` 虚函数槽为 15；均通过当前引擎代码及运行验证确认。此 gamedata 不适用于 Windows，游戏引擎更新后应重新核对布局。标签不超过 31 个 UTF-8 字节。

社区方案参考：[A2S Info Change](https://forums.alliedmods.net/showthread.php?p=2801009)。本服是独立的轻量实现，不包含该插件或新增扩展。

## 验证与备份

- 全部 14 个 custom SourcePawn 源文件编译通过，仅公共 include 的既有弃用警告。备份目录中轮廓插件的旧乱码源码已用游戏目录内正常副本修复；运行中的二进制保持不变。
- 空服重启后 A2S 查询名称、游戏、中文地图正确，AppID 550、12 人容量和 VAC 正常。
- 中文地图已验证跨章节、跨战役切换并恢复 C2 第一章；当前模式仍为 ZoneMod。
- 完整私密恢复包已生成，SQLite 快照一致性和隔离解包校验通过。该包包含实际插件、配置、数据及恢复工具，不含可重新下载的基础游戏。此次不声称做过异机完整启动演练。
- GitHub 仅保存脱敏源码及配置，不上传完整恢复包、数据库、管理员身份、密码或 SSH 凭据。
- 服务器浏览器显示已经用户确认；此次未新增真人多客户端测试。
