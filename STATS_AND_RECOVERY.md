# 贡献排行与恢复（规则版本 1）

SQLite 与游戏服运行在同一台云服务器；不依赖家用电脑在线，不需要购买数据库服务。

## 使用与计分

`!rank` / `!stats` 查看个人排行与分项，`!top` 查看前十名。累计贡献受游玩时长影响，不是竞技段位。

只有 readyup 正式开局、双方各至少 2 名真人的半场才可计分。半场中人数不足、开启作弊或管理员刷克/接管 AI Tank，将排除该半场。刷克/接管还将整个当前章节标记为练习；管理员可用 `!practice` 主动标记。准备阶段、无效重开、单人练习不加分。换至下一章节后恢复正常资格；同一章节重载仍保留练习标记。

半场结束累计后取整：小丧尸每 20 只 +1（每半场最多 +10）；普通特感伤害每 100 +1；打 Tank 伤害每 500 +1；解控队友每次 +3；扶起队友每次 +2；普通特感输出每 10 +1；Tank 输出每 20 +1；友伤每 10 −1。排除过量伤害和对倒地生还者持续磨血，救援去重，扶起同一目标有 30 秒计分间隔。击杀特感另作统计，不叠加最后一击奖励。

以 SteamID64 识别玩家，断线重连保留半场内身份。每半场唯一编号，事务及唯一键防止重复结算。SQL 写入失败的结算保存在 `addons/sourcemod/data/jjd-stats-pending`，每分钟或插件重新加载时重试。崩溃前尚未结束的半场不结算。

## 自动备份

云端 `l4d2-backup.timer` 每天服务器时间 04:30 左右生成完整恢复包，最多保留最新 30 个。使用 SQLite 在线备份 API 创建一致性快照，替换归档中的实时数据库；包含实际配置、插件、数据库、脚本与匹配的编译工具。基础游戏不包含。

本机计划任务 `Jiaojiedi-Private-Backup-Sync` 在登录 Windows 后及每小时主动使用 SSH 下载最新包。电脑关机时不运行；开机联网后补拉最新有效版本，不逐个下载离线期间的所有历史包。无需内网穿透，切换热点也可用，只需能访问云端 SSH。

下载到临时文件，大小/SHA256 校验成功才替换最新清单；失败保留旧备份并于下次触发重试。状态在本地 `backups/sync-status.json`。本机最多保留 30 个新版完整包，早期旧格式归档保留。任务仅在用户登录期间运行，不存储 Windows 密码。

云端失败可查 `journalctl --user -u l4d2-backup.service`。如长时间没生成新包，应检查该日志；此版本没有邮件/推送报警。电脑长期离线且旧云被释放时，仍只能恢复到最后下载成功的时间点。

配置大改后执行 `python3 ~/deploy/backup-server.py`，再在本机运行同步脚本。到期前停止游戏写入，生成并下载最后一份包。

## 新云恢复

1. 选择 Ubuntu 22.04 x86_64，准备 GitHub 脚本、最新完整恢复包和对应 `.json` 清单。
2. 新云 root 执行 `bash bootstrap-server.sh GAME_USER`，安装依赖和基础游戏。脚本不会覆盖已有模组安装；按提示为该用户配置 SSH 公钥。
3. 以游戏用户登录，上传恢复包与清单，先运行：

```sh
python3 restore-server.py --archive FULL.tar.gz --manifest FULL.tar.gz.json --verify-only
```

4. 然后运行：

```sh
python3 restore-server.py --archive FULL.tar.gz --manifest FULL.tar.gz.json --new-ip NEW_SERVER_IP
```

脚本验证哈希、安全解压、检查 SQLite 完整性和版本、调整原路径/IP、重新编译插件并启用服务。编译工具随私密恢复包保存，不需要追随上游最新版本。

5. 云安全组允许 UDP 27015、欢迎页 TCP 80 和实际 SSH 端口。检查欢迎页、ZoneMod、管理员、积分库与真人游玩。
6. 本机更新 SSH 配置中的新 IP/端口/用户，并通过可信控制台核对新主机指纹。同步脚本继续使用同一个 SSH 别名。

数据库及真实配置不公开上传 GitHub；仅提交源码、结构和恢复流程。隔离解包测试不能替代在新云下载游戏并完成真人验收。


## Tank 工具 1.1.1

管理员新刷 Tank 会清除前一只 Tank 遗留的全局控制次数，首次接管按一控计数；人工接管不额外消耗次数，也不返还已消耗的次数。已有存活 Tank 时拒绝同时再刷，避免全局计数相互影响。开场传克保留原计数；正式怒气耗尽后的二控仍由 ZoneMod Tank Control 管理。

ZoneMod 自带后退冻结怒气：最靠前生还者的地图流程位置每后退达到 7%，增加 4 秒冻结时间；不是固定物理距离。自动验收覆盖新克初始化和已有计数保护，真人完整怒气周期仍需实测。


### 已知限制：管理员刷克二控（2026-09-07）

管理员真人复测仍报告 `!spawntank` 生成的 Tank 只有一控，此问题尚未解决；本阶段暂停继续排查。1.1.1 的自动检查只证实新克计数清零和已有计数保护，不能证明完整二控正常。正式 ZoneMod Tank Control 源码未修改，但尚未完成正式对局完整怒气周期的真人回归，不能据此保证正式二控完全不受影响。发布前应补做正式自然刷克及开场传克后的二控验证。

## 2026-09-08：轮廓、Tank 预告与娱乐投票

- 生还者达到本服黑白状态且仍站立存活时，感染者可见蓝色轮廓。恢复正常、死亡或换队后移除；不修改生还者自己的原生轮廓。
- ZoneMod 后退冻结保留原规则：最靠前生还者每后退 7% 地图流程，增加 4 秒怒气冻结。实际触发时聊天提示增加秒数，一次跨多个档位会累计。
- `!tankline`：查看 Tank 预告说明。仅感染者和旁观者接收白色预计流程边界和淡蓝预测区域模型，生还者不接收。位置来自当前回合刷克流程参数与导航网格；最终出生点由导演决定，模型不是已确定的出生坐标。剧情／救援 Tank 和无法获得有效流程参数的地图不显示。本功能不能保证在所有地图复刻 C2 改的固定出生点。
- `!vote` 新增暴雨、装饰头饰和关闭娱乐效果。沿用参赛真人严格过半、20 秒投票、60 秒冷却。
- 娱乐功能默认关闭；投票通过后本章节标记为练习，停止排行计分。即使关闭效果，本章节也不恢复计分；换到下一章节重置。娱乐状态在同一章节的配置重载后保留。
- 暴雨为视觉降雨，不修改武器数值，不附加伤害雷击。头饰使用游戏自带小矮人和交通锥，`!hat` 选择或摘下；无碰撞，自己的第一人称和跟随自己的第一人称旁观不显示头饰。
- 云端每日恢复包继续生成；电脑的 `Jiaojiedi-Private-Backup-Sync` 计划任务目前已停用。双击本地 `enable-backup-sync.cmd` 即可恢复登录后／每小时同步，并立即同步一次。需要迁移前仍应获取最新完整包。

实现：`jjd_visuals`、`jjd_fun`、`jjd_server` 1.1.0，以及保留作者署名的 `l4d2_tankrage` 1.0.3-jjd1 小改版。`install-features.py` 纳入日常部署和新云恢复；不叠加第二个 Tank 怒气插件。此前管理员刷克只有一控的已知问题不在本次修改范围内，仍需单独实战验收。

## 2026-09-08：空服误清退与预告可见性修复

空服维护改为唯一的自动清理入口。禁用 Confogl 在玩家离开约 60 秒后的自动模式卸载，保留管理员主动换模式／重置模式。比赛配置持续加载；关闭引擎空服休眠，允许只有感染者或旁观者的练习场景。

后台使用单调时钟确认连续空服至少 30 分钟，通常在最后真人离开约 30～32 分钟后处理；观察中断、状态不完整或维护插件不可用时放弃重启并重新计时。已连接、正在加载和旁观的真人均阻止重启；两次轮询间的短暂进出通过连接代数识别，也会重新计时。

后台不再直接发起 systemctl 重启，而是请求游戏线程即时核对连接代数、真人数和最后活动时间，全部通过后执行 quit，由 systemd 拉起新进程。没有延迟关闭倒计时，不使用 sv_shutdown；重启后的全新空服不反复重启。13 项自动回归及真实空服重启测试通过，测试使用模拟活动时间，并非等待完整 30 分钟。

Tank 预告边界改为竖立的白色斜纹警戒带，采用分队发送的短寿命效果，距离过滤与数量上限限制开销。预测 Tank 模型和黑白蓝色轮廓强制经过逐客户端发送检查，清除可能绕过 SDKHook_SetTransmit 的优化标志；玩家换队时删除旧模型，避免缓存的轮廓继续显示。预测 Tank 和警戒带仅感染者／旁观者可见，生还者（包括死亡状态）不在接收名单。仍然是流程预告，并不保证导演的最终出生坐标。

版本：jjd_server 1.1.1、jjd_visuals 1.1.0、Confogl 2.5.0-jjd1。install-idle-fix.py 对原有 Confogl 模块施加可重复应用的小补丁并编译；上游版本变化时停止并要求维护检查。维护脚本与恢复流程已包含此步骤。实际客户端显示需要真人验收，不能只凭过滤函数返回值确认。

电脑自动备份同步仍暂停；云端每日备份继续。管理员刷克二控的已知问题没有在本次修复。

## 斜纹整理与访客查询

jjd_visuals 1.2.0 将邻近导航边界按通道主方向合并，统一斜纹相位、间距和底部高度，线宽减半、斜纹高度降到 48 单位；过滤短碎片、明显跨楼层连线以及没有地面支撑的片段。可见性修复保持不变：仅感染者和旁观者接收预告。普通地图仍属于估算边界，不等同于专门制作地图里的精确固定触发面。

Windows 私密目录新增 view-server-visitors.cmd，双击显示当前真人、近 7 天认证玩家和最近 20 条进出事件，然后每 5 秒增量读取新记录；Ctrl+C 结束。脚本主动通过已有 SSH 配置连接云端，换热点可用，不需要内网穿透，也不会开启备份任务。一次查询可运行 view-server-visitors.ps1 -Once；-Days 0 查看全部仍保留的日志。

云端脚本为 deploy/visitor-status.py，依据游戏现有日志中的 Steam 身份识别真人，排除 BOT；管理员标识读取实际 admins_simple.ini。显示昵称、时间和 SteamID，不显示 IP。换图也可能产生新的进入事件，因此事件次数不是独立访问次数。没有历史记录不能证明从未连接；旧日志不完整时无法追溯已丢失的部分。没有新增数据库或无人值守监控任务。
