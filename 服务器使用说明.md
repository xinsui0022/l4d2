# 交界地 · 求生之路 2 药抗服

已部署：ZoneMod 2.9.1b，100 Tick 启动配置，4v4，8 个玩家位，官方地图。
用户已确认 MVP 能进服游玩。本次增加欢迎页、投票菜单、管理员绑定及空服维护；完整 4v4 投票和游戏内欢迎页显示仍需实机体验。实际验收记录见 verification/ 和 DEPLOYMENT_NOTES.md。

## 本服规则与欢迎页
- 服务器名称：交界地。
- 欢迎页使用提供的星空原图，公告为“本服务器由五郎、心碎提供”。
- 公告排版：下方大图无叠加文字；上方横幅顶部居中显示 44px 服名与 22px 公告。
- 预览：http://YOUR_SERVER_IP/l4d2/welcome.html 。这是进服欢迎页，不会替换玩家本地的游戏主菜单。
- 进程启动或空服维护重启后，从 C2 黑色嘉年华第一章开始。
- 正常对局仍逐章进行。整场战役结束后等待 30 秒，默认开启 C5 教区第一章，双方比分清零。
- 玩家可投票修改下一场战役，仅生效一次；下一次结算恢复默认 C5。
- 保留 ZoneMod 的 C6→C7、C9→C14 战役串联和其中的正常计分。
- 真人离开后连续空服 30 分钟，后台检查触发一次重启；检查间隔约 60 秒，因此通常在空服 30～31 分钟左右执行。有人加入就重新计时；重启后一直无人不会重复重启。旁观者也算真人。

## 连接
游戏开发者控制台：
```
connect YOUR_SERVER_IP:27015
```
登录服务器（本机 PowerShell）：
```
ssh -F YOUR_LOCAL_WORKSPACE/ssh_config_l4d2 your-l4d2-host
```
私钥留在本机，不上传到云服务器、不打包进游戏文件、不加入 Git。

## 管理
以下命令在云服务器的 l4d2 用户下运行，不需要 sudo：
```
systemctl --user status l4d2 --no-pager
systemctl --user restart l4d2
systemctl --user stop l4d2
systemctl --user start l4d2
tail -n 80 /home/l4d2/logs/server-console.log
```
控制台命令通过本机 RCON 工具发送，它自动读取服务器本地配置，不需要手工输入密码：
```
python3 /home/l4d2/deploy/rcon.py status
python3 /home/l4d2/deploy/rcon.py sm plugins list
python3 /home/l4d2/deploy/rcon.py sm exts list
python3 /home/l4d2/deploy/rcon.py changelevel c2m1_highway
python3 /home/l4d2/deploy/rcon.py sm_jjd_newcampaign c5m1_waterfront
python3 /home/l4d2/deploy/rcon.py sm_jjd_status
```
换图、重启会中断当前对局，安排在空服时操作。
`sm_jjd_newcampaign` 只接受官方战役起始图，并清零比分。ZoneMod 在比赛配置启用期间锁住插件热加载；更新插件后应空服重启，不直接使用 `sm plugins reload`。

空服维护和欢迎页服务：
```
systemctl --user status l4d2-idle-check.timer l4d2-motd --no-pager
journalctl --user -u l4d2-idle-check.service -n 10 --no-pager
```
维护脚本独立于游戏插件运行，休眠和 ZoneMod 卸载插件不会使检查停止。不要手工修改 `jjd_idle_*` 活动记录。

## 游戏命令
- `!ready` / `!r`：准备。
- `!unready`：取消准备。
- `!pause`：暂停；`!unpause`：本队准备恢复。
- `!spec`：转旁观。
- `!match`：模式投票菜单。
- `!tank` / `!boss`：查看 Tank 等信息。
- `!vote`：本服投票菜单，支持立即换战役、选择下一场、从第一章重开当前战役。
- `!nextmap`：查看战役结束后去哪里。
- `!welcome`：打开欢迎页。
- `!admin`：管理员菜单，已绑定你的 Steam 账号 `YOUR_STEAM64`（最高权限，免额外密码）。
- `!forcestart` / `!fs`：管理员在准备阶段强制开始倒计时，现有 readyup 已自带。
- `!givetank 玩家名`：管理员指定待分配的 Tank 人选，目标须为感染者队真人。该命令更新分配队列，不会立即转交已经由玩家控制的 Tank。
- `!pass` / `!tankpass`：开场传克菜单。接到 Tank 后自动弹出队友名单、随机队友、自己玩；10 秒内选一次，超时默认自己玩，每秒刷新倒计时；拳击对生还者造成伤害或投出任意石头后立即关闭。交接保留血量、怒气和引擎交接计数。只有当前 Tank 能使用。
- `!spawntank`：仅最高权限管理员，瞄准附近空旷平地生成一只 Tank；也可从 `!admin` → 服务器命令进入。
- `!taketank`：仅最高权限管理员，先加入感染者队，再从菜单选择接管一只 AI Tank。用于练习；不能抢其他真人的 Tank。

本服 `!vote` 使用游戏自带的 F1/F2 投票界面；只允许生还者/感染者队的真人发起和投票，旁观者不参与。需要发起时全部参赛真人的严格过半同意，例如 8 人需 5 人。20 秒投票时间，两次发起至少相隔 60 秒；立即换战役/重开通过后等待 5 秒执行。
此菜单由本地 `jjd_server` 插件提供，使用已经安装的 BuiltinVotes 扩展；没有同时安装第二套自动换战役插件。

服务器应自动加载 ZoneMod；准备阶段等待玩家准备是正常行为。
普通感染者死亡消失使用 ZoneMod 自带插件。开场传克和管理员刷克由本服独立 `jjd_tank_tools` 提供，保留 ZoneMod Tank Control，不叠加上游 Tank Pass 或整套 Admin System。
传给仍在操作普通特感的队友时，其原特感先交给 AI，队友接管 Tank。开场传克仅一次，避免反复交接；被传入的队友不会再获得新的传克窗口。
客户端采用竞技一致性检查；若提示不兼容插件或文件，应按提示停用对应本地模组再重连。

## 文件位置
- 服务端：`/home/l4d2/steamcmd/l4d2`
- 主配置：`/home/l4d2/steamcmd/l4d2/left4dead2/cfg/server.cfg`
- RCON 私密配置：同目录 `private_server.cfg`，不要公开。
- 模式配置：`left4dead2/cfg/cfgogl/zonemod/`
- SourceMod 日志：`left4dead2/addons/sourcemod/logs/`
- 用户服务：`/home/l4d2/.config/systemd/user/l4d2.service`
- 部署与控制脚本：`/home/l4d2/deploy/`
- 首次迁移遗留的旧服存档：`/home/l4d2/l4d2-backups/pre-zonemod-20260906/`，仅供参考，不再维护，不用于当前服务回滚。
- 官方模组源码：`/home/l4d2/l4d2-mod/`
- 固定版本记录：`/home/l4d2/deploy/installed-version.json`
- 本服定制源码与图片：`/home/l4d2/custom/`，本地对应 `YOUR_LOCAL_WORKSPACE/custom/`。
- 定制插件：`left4dead2/addons/sourcemod/plugins/jjd_server.smx`，比赛加载项位于 `cfg/sharedplugins.cfg`。
- 管理员配置：`left4dead2/addons/sourcemod/configs/admins_simple.ini`。
- 空服维护：`/home/l4d2/deploy/idle-watch.py`；进程活动记录：同目录 `idle-state.json`。
- 欢迎页通过已有 nginx 的 `/l4d2/` 路径转发到本机 `127.0.0.1:18080`，仅提供三份公开素材。

华为云安全组需要允许游戏入站 UDP 27015，SSH 使用 TCP YOUR_SSH_PORT。RCON 通过 SSH 在服务器本地调用，无需为远程 RCON 额外开放 TCP 27015。
SteamCMD validate 应在安装模组前执行；未来升级应先停止游戏、保存当前有效的自定义配置，再校验游戏并重新应用模组配置。原有旧服文件不再备份或维护。
`deploy/apply-customization.py` 可重复应用当前定制配置；基础配置脚本在定制文件齐全时也会应用这层配置。更新 SourceMod/ZoneMod 后须用对应编译器重新编译本服插件，并重新验收。
